#include "cast/Transform/Transform.h"

using namespace cast;
using namespace cast::draft;

GateMatrixPtr transform::convertGate(ast::GateApplyStmt* astGate,
                                     ast::ASTContext& astCtx) {
  assert(astGate != nullptr);
}
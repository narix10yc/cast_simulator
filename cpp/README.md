# C++ Layer

This directory is reserved for code that remains in C++ after the Rust migration starts.

- `include/` is for the stable public C++ API and backend-specific wrappers.
- `src/` is for LLVM, CUDA, FPGA, and FFI bridge implementation.
- `tests/` is for C++ bridge and integration tests.
